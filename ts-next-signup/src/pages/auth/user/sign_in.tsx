import styled from '@emotion/styled';
import { FormWrapper, FormFieldset, FormLegend } from '@/components/atoms';

const SignIn = () => {
  return <Container></Container>;
};

const Container = styled.section(() => {
  return {};
});

export default SignIn;
